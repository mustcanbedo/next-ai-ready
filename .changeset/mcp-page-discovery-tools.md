---
"@next-ai-ready/mcp": minor
---

Add graph-backed `list_pages`, `get_page`, and deterministic local `search_pages` MCP tools. They register automatically when `registerAiReady` receives a SemanticGraph and enforce bounded pagination, query, result, and route inputs.
