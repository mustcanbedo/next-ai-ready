---
title: 国际化与 AI 友好 URL
summary: 多语言站点的 middleware、locale 前缀与 SemanticGraph 路由策略。
order: 8
questions:
  - q: locale 前缀如何影响 AI 路由？
    a: 每个已识别的 locale 路径都是独立路由，graph 同时写入 locale 元数据和 routesByLocale 索引。
  - q: MCP 页面工具可以按 locale 过滤吗？
    a: 可以。search_pages 支持可选的 locale 过滤；list_pages 仍是带 locale 元数据的全站索引。
  - q: middleware 应 redirect AI 产物 URL 吗？
    a: 不应。matcher 需排除带点路径、/api、_ai-ready，避免爬虫被重定向。
tags:
  - how-to
  - best-practices
updatedAt: '2026-08-01'
author: next-ai-ready 团队
---

# 国际化与 AI 友好 URL

`next-ai-ready` 按 `content` glob 扫描 MDX，并将文件路径映射为路由。多语言站点请在路由中包含 locale（例如 `/zh/docs/install`）。

## 推荐目录结构

```text
content/
  en/
    docs/
      introduction.mdx
      installation.mdx
      guides/quickstart.mdx
  zh/
    docs/
      introduction.mdx
```

```js
export default defineConfig({
  content: ["content/{en,zh}/**/*.mdx"],
});
```

`en`、`zh` 等已识别的路由前缀会写入页面节点的 `locale` 字段。生成的 `SemanticGraph.routesByLocale` 还会将语言内相对路由映射回页面 id，同时保留扁平 `routes` 以兼容现有消费者。

MCP 页面结果会返回 locale。调用 `search_pages` 时传入 `locale` 可限制检索语言；`list_pages` 仍保持确定性的全站索引，客户端可依据 locale 元数据或路由前缀浏览。

## Middleware

若将 `/docs` 重定向到 `/en/docs`，请在 matcher 中排除 AI 产物路径，避免 agent 被重定向：

```ts
export const config = {
  matcher: ["/((?!_next|_ai-ready|api|.*\\..*).*)"],
};
```

需保持可访问的路径：

- `/_ai-ready/*`
- `*.md`、`*.ai.json`（由 `withAiReady()` rewrite）
- `/llms.txt`、`/openapi.json`、`/tools.json`、`/.well-known/ai-plugin.json`

## llms.txt

可用 config `sections` 在 `llms.txt` 中优先展示某一语言，或为每种语言单独部署。

## 当前限制

自动生成 `Available Languages`、Markdown alternate frontmatter 与 `hreflang` 响应链接仍在规划中。MCP `search_pages` 的 locale 过滤已经可用。当前边界见 [Phase 6 设计](https://github.com/mustcanbedo/next-ai-ready/blob/main/docs/phase6-design.md)。
