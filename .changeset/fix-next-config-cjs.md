---
"next-ai-ready": patch
"@next-ai-ready/next": patch
---

Add a CommonJS-compatible `next-ai-ready/config` export so `init` works with
the `next.config.ts` files generated by current Next.js applications.
