{
  "name": "@next-ai-ready/llms",
  "version": "0.1.0-alpha.2",
  "description": "llms.txt / llms-full.txt / per-route Markdown generators.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@next-ai-ready/core": "workspace:*",
    "@next-ai-ready/semantic": "workspace:*"
  },
  "devDependencies": {
    "@next-ai-ready/mdx": "workspace:*",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
